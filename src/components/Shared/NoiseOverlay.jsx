/**
 * NoiseOverlay - Subtle texture for depth
 * 
 * Adds a film grain-like texture overlay to create
 * visual depth and a premium feel.
 */
const NoiseOverlay = () => {
  return (
    <div 
      className="noise-overlay"
      aria-hidden="true"
    />
  )
}

export default NoiseOverlay

